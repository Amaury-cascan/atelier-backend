{% extends 'base.html.twig' %}

{% block title %}Modifier l'image{% endblock %}

{% block body %}
    <h1 class="mb-4">Modifier l'image</h1>

    {{ include('picture_presentation/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="d-flex gap-2">
        <a href="{{ path('app_picture_presentation_index') }}" class="btn btn-secondary">Retour à la liste</a>
        {{ include('picture_presentation/_delete_form.html.twig') }}
    </div>
{% endblock %}
