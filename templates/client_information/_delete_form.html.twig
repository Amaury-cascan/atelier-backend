<form method="post" action="{{ path('app_client_information_delete', {'id': client_information.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client_information.id) }}">
    <button class="btn btn-danger">X</button>
</form>
