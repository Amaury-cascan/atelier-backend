{% extends 'base.html.twig' %}

{% block title %}Nouvelle prestation{% endblock %}

{% block body %}
    <h1 class="mb-4">Créer une nouvelle prestation</h1>

    <div class="card mb-4">
        <div class="card-body">
            {{ include('service/_form.html.twig') }}
        </div>
    </div>

    <a href="{{ path('app_service_index') }}" class="btn btn-secondary">Retour à la liste</a>
{% endblock %}